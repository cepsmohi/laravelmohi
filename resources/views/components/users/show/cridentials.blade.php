<div class="frows gap-2">
    <x-ui.icon icon="mail" width="w-10"/>
    <div class="stitle lowercase">{{ $user->email }}</div>
</div>
<div class="frows gap-2">
    <x-ui.icon icon="phone" width="w-10"/>
    <div class="stitle">{{ $user->phone }}</div>
</div>
